TestNamespace
{
    theory
    {
        axiom Test() {
            t: tplFieldElem[1 ~ n]

            // because we are in an axiom, we need at least one predicate to avoid causing syntax errors
            true
        }
    }
}