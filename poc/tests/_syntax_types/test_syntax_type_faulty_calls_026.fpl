TestNamespace
{
    theory
    {
        axiom Test()
        {
            Test(Field(fieldSet: tplFieldSet, fieldAdd: BinOp(tplFieldElement, tplFieldElement), fieldMul: BinOp(tplFieldElement, tplFieldElement) ))
        }
    }
}