TestNamespace
{
    theory
    {
        func BinOp() -> tplSetElem
        {
            optional pred IsAssociative(x1,y1: tplSetElem)
            {
            }
        }
    }
}

##############################
This should not cause FplUnusedVariable, since the variables x,y are
declared inside a signature of an intrinsically declared predicate property (x1,y1)
